#include <stdio.h>
#include <string.h>
/*
int main() {

	int T = 0;
	scanf("%d", &T);

	int R = 0;
	char buffer[21] = { 0, };
	for (int i = 0;i < T;i++) {
		scanf("%d", &R);
		scanf("%s", buffer);
		int len = strlen(buffer);
		for (int j = 0;j < len;j++) {
			for (int k = 0;k < R;k++) {
				printf("%c", buffer[j]);
			}
		}
		printf("\n");
	}

}
*/