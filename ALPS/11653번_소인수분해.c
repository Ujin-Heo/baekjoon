#include <stdio.h>
/*
int main() {

	int N = 0;
	scanf("%d", &N);
	int N_cpy = N;

	int divider = 2;
	if (N == 1) {
		printf("");
	}
	else {
		while (divider <= N) {
			if (N_cpy % divider == 0) {
				printf("%d\n", divider);
				N_cpy = N_cpy / divider;
			}
			else {
				divider++;
			}
		}
	}
}
*/